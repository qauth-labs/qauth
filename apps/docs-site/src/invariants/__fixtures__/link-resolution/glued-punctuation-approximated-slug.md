See [the redirect section](/guide/#redirect-to-oauth-authorize) — the OLD
hand-rolled approximation's WRONG guess at the anchor (it inserts a hyphen
between glued words that the real `github-slugger` output does not have).
