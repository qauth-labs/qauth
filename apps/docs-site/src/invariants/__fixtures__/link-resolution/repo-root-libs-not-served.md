See the [mcp-guard README](/libs/fastify/plugins/mcp-guard/README.md) — the
exact real-content shape this rule's generalization exists to catch: a real
repository file under `libs/`, which the deployed site publishes no more than
it publishes `docs/`.
