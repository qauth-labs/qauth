{
  "name": "fastify-plugin-db",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/fastify/plugins/db/src",
  "projectType": "library",
  "tags": ["scope:fastify", "type:db"],
  "// targets": "to see all targets run: nx show project fastify-plugin-db --web",
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "{workspaceRoot}/coverage/{projectRoot}"
      }
    }
  }
}
