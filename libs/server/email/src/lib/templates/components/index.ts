/**
 * React Email primitives, vendored from resend/react-email (MIT).
 *
 * Replaces the deprecated `@react-email/components` barrel (#333). Only the
 * eight primitives our templates use are vendored; add more from upstream as
 * templates need them. See `./README.md` for provenance and `./LICENSE` for the
 * upstream licence.
 */
export { Body, type BodyProps } from './body';
export { Button, type ButtonProps } from './button';
export { Container, type ContainerProps } from './container';
export { Head, type HeadProps } from './head';
export { Html, type HtmlProps } from './html';
export { Preview, type PreviewProps } from './preview';
export { Section, type SectionProps } from './section';
export { Text, type TextProps } from './text';
